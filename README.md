# Golang Seed

Run the code:

```sh
make run
```

Format:

```sh
make format
```

Lint:

```sh
make lint
```

Test:

```sh
make test
```

Run format, lint, and test on changes (requires `ripgrep` and `entr`):

```sh
make watch
```

Re-create module / `go.mod`:

```
go mod init
```